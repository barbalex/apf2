query Query {
  tree @client {
    name
    activeNodeArray
    openNodes
    apFilter
    nodeLabelFilter
  }
  tree2 @client {
    name
    activeNodeArray
    openNodes
    apFilter
    nodeLabelFilter
  }
}