query Query {
  user @client {
    token
  }
  tree @client {
    name
    activeNodeArray
    openNodes
    apFilter
    nodeLabelFilter
  }
  tree2 @client {
    name
    activeNodeArray
    openNodes
    apFilter
    nodeLabelFilter
  }
  urlQuery @client {
    projekteTabs
    feldkontrTab
  }
  copyingBiotop @client {
    id
  }
}
