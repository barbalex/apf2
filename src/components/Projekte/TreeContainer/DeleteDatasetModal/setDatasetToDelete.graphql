mutation setDatasetToDelete(
  $table: String!
  $id: UUID!
  $label: String!
  $url: String
) {
  setDatasetToDelete(table: $table, id: $id, label: $label, url: $url) @client {
    datasetToDelete @client {
      table
      id
      label
      url
    }
  }
}
