query userById($id: UUID!) {
  userById(id: $id) {
    id
    name
    email
    role
    pass
    adresseId
    adresseByAdresseId {
      id
      name
    }
  }
  allAdresses {
    nodes {
      id
      name
    }
  }
}
