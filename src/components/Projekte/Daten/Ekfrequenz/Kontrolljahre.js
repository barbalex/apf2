import React, { useState, useEffect } from 'react'

/**
 * build an array of fields
 * set state on mount
 * write to db on blur
 */
const Kontrolljahre = kontrolljahre => {
  console.log('Kontrolljahre:', kontrolljahre)
  return <div>{'Kontrolljahre (TODO)'}</div>
}

export default Kontrolljahre
