query QkQuery(
  $berichtsjahr: Int
  $isBerichtsjahr: Boolean!
  $projekt: UUID!
  $isProjekt: Boolean!
  $ap: UUID!
  $isAp: Boolean!
) {
  zielOhneJahr: allZiels(filter: {jahr: {isNull: true}}) {
    nodes {
      id
      apId
      apByApId {
        id
        projId
      }
    }
  }
}