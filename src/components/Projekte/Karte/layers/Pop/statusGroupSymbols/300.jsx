import { memo } from 'react'

import Icon from './300.svg'
import { IconContainer } from '../../../../../NavElements/IconContainer'

export const PopIcon300 = memo(() => (
  <IconContainer>
    <Icon />
  </IconContainer>
))
