import { memo } from 'react'

import Icon from './300Highlighted.svg'
import { IconContainer } from '../../../../../NavElements/IconContainer'

export const PopIcon300Highlighted = memo(() => (
  <IconContainer>
    <Icon />
  </IconContainer>
))
