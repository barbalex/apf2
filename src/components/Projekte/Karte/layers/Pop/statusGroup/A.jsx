import { memo } from 'react'

import Icon from './a.svg'
import { IconContainer } from '../../../../../NavElements/IconContainer.jsx'

export const PopIconA = memo(() => (
  <IconContainer className="iconContainer">
    <Icon />
  </IconContainer>
))
