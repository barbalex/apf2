import { memo } from 'react'

import Icon from './q.svg'
import { IconContainer } from '../../../../../NavElements/IconContainer'

export const PopIconQ = memo(() => (
  <IconContainer>
    <Icon />
  </IconContainer>
))
