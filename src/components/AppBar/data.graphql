query AppBarQuery {
  user @client {
    name
    token
  }
  urlQuery @client {
    projekteTabs
    
  }
  tree @client {
    activeNodeArray
  }
  datasetsDeleted @client {
    table
    id
    label
    url
    data
    time
  }
}
