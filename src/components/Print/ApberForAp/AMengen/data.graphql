query apById($apId: UUID!) {
  apById(id: $apId) {
    id
    oneLPop: popsByApId(filter: {status: {notEqualTo: 300}}) {
      nodes {
        id
        tpopsByPopId(condition: {apberRelevant: 1}) {
          totalCount
        }
      }
    }
  }
}
