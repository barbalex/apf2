query EkfListQuery($userName: String!, $jahr: Int!) {
  allUsers(filter: { name: { equalTo: $userName } }) {
    nodes {
      id
      name
      adresseByAdresseId {
        id
        tpopkontrsByBearbeiter(
          filter: {
            typ: { equalTo: "Freiwilligen-Erfolgskontrolle" }
            jahr: { equalTo: $jahr }
          }
        ) {
          nodes {
            id
          }
        }
      }
    }
  }
}
