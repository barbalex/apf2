query Query {
  isPrint @client
  ekfYear @client
  user @client {
    name
    token
  }
  urlQuery @client {
    projekteTabs
    feldkontrTab
  }
  tree @client {
    name
    activeNodeArray
    openNodes
  }
}
