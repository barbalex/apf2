import get from 'lodash/get'

export default async ({ tpopId, ekfrequenzCode, client, store }) => {
  const { enqueNotification } = store
  // 1. query all ekplans beginning with ekfrequenzStartJahr
}
