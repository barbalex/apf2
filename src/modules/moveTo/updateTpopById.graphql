mutation updateTpopById($id: UUID!, $popId: UUID) {
  updateTpopById(
    input: {
      id: $id
      tpopPatch: {
        popId: $popId
      }
    }
  ) {
    tpop {
      id
      popId
    }
  }
}